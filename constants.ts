// Optional: For future expansion or if we want to fallback if Gemini fails (though we rely on Gemini for now)
export const FALLBACK_FUEL_PRICES = {
  PETROL: 100,
  DIESEL: 90,
  CNG: 80,
  ELECTRIC: 10,
};
